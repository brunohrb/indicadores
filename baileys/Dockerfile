FROM atendai/evolution-api:latest

# A Evolution API já vem configurada — só expõe variáveis de ambiente no Railway:
# AUTHENTICATION_API_KEY   → chave secreta da API (invente uma string aleatória)
# AUTHENTICATION_EXPOSE_IN_FETCH_INSTANCES → true
# SERVER_URL               → URL pública do Railway (ex: https://texnet-bot.up.railway.app)
# WEBHOOK_GLOBAL_ENABLED   → true
# WEBHOOK_GLOBAL_URL       → URL da sua Supabase Edge Function whatsapp-webhook

EXPOSE 8080
